# OpenAI API Key (get from https://platform.openai.com/api-keys)
OPENAI_API_KEY=sk-your-openai-api-key-here

# For local development (Vite requires VITE_ prefix for client-side vars)
VITE_OPENAI_API_KEY=sk-your-openai-api-key-here

# Server Port (Railway sets this automatically in production)
PORT=3000

# Note: In production (Railway), only set OPENAI_API_KEY
# The frontend will use the /api/chat endpoint instead of direct API calls
