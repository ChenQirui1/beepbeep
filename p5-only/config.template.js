// Configuration template for API credentials
// 1. Copy this file and rename it to "config.js"
// 2. Replace the placeholder values with your actual API credentials
// 3. Never commit config.js to version control (it's in .gitignore)

const CONFIG = {
  // Get your API key from: https://console.anthropic.com/
  CLAUDE_API_KEY: 'sk-ant-api03-...',

  // API endpoint (usually no need to change this)
  CLAUDE_API_URL: 'https://api.anthropic.com/v1/messages'
};
